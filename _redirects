/contact https://links.evanhollander.org 301

# The demo board. A rewrite rather than a redirect so the address stays /demo;
# index.html's relative asset paths still resolve against / because /demo has no
# trailing slash. The trailing-slash form would resolve them against /demo/, so
# send it back to the canonical path first.
/demo/ /demo 301
/demo /index.html 200
