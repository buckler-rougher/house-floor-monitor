# Wrangler local state (contains account ID and cached credentials)
.wrangler/

# Environment / secret files
.env
.env.*
.dev.vars

# Claude Code local overrides (personal settings, not team config)
.claude/settings.local.json

# macOS
.DS_Store

# Node
node_modules/

# Lag poller output
dev/proceedings-lag.jsonl

# Agent instructions: local only. These deploy to the site root and are
# served publicly — house-floor was exposing its deploy rules at /CLAUDE.md.
CLAUDE.md
AGENTS.md

# Agent instructions. Cloudflare Pages serves .claude/ publicly (verified:
# /.claude/launch.json returns 200), and these describe how to drive the app
# and name internals. Same reason AGENTS.md is ignored.
.claude/skills/
